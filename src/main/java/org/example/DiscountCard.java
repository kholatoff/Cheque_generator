package org.example;

public class DiscountCard {
    public int id;
    public int discount;

    public DiscountCard(int identifier, int pricedown) {
        id = identifier;
        discount = pricedown;
    }
}
